node_modules
.next
.env
.env.local
.env*.local

# All persistent data now lives in Supabase Postgres + Storage. The local
# data/*.json files and public/uploads/* are dev snapshots only — not source
# of truth, not safe to commit (PII, passwords, etc.).
data/users.json
data/orders.json
data/coupons.json
data/theme.json
data/products.json
public/uploads/*
!public/uploads/.gitkeep
